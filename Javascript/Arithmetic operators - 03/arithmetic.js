//let student = 30

//student = student + 1;
//student = student - 1;
//student = student **2;
//student = student / 2;
//student = student % 9; (The output should be 3.3334. It shows 3 only becuase the remainder cannot be in point in "%")

//student += 1;
//student -= 1;
//student *= 1;         (Same as the above expressions but in a short method)
//student /= 2;
//student %= 9;

//student++;
//student--;
   //console.log(student);

/*
    operator precedence:-
     1. parenthesis()
     2. exponents
     3. multiplication, subtraction and mudulo (Or modulus)
     4. addition and subtraction
*/

//let result = 1 + 2 * 3 + 4 ** 2;
//let result = 4 % 2 * 3 + 1 - 3;
//let result = 4 / 2 + 3 - 3 * 1 + (2 + 2);
let result = 6 / 2 ** (2 + 5);
console.log(result);    

